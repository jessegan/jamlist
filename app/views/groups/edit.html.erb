<h1>Edit Group</h1>

<%= render partial: 'form', locals: {btn_name: "Save"} %>

<br>

<% if owner_of(current_group) %>
    <%= button_to "DELETE GROUP", current_group, method: :delete, data: {confirm: "Are you sure you want to delete #{current_group.name}?"} %>
<% end %>